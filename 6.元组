1.定义
不可修改的列表叫做元组
注意：元组中的某个数据不能被修改，但是元组整体可以被修改。例如
>>> dimensions=(200,50)
>>> for value in dimensions:
	print(value)

	
200
50
>>> dimensions=(400,100)
>>> for value in dimensions:
	print(value)

	
400
100

元组看起来像列表
区别：使用圆括号，而不是方括号来标识。
定义元组后，可以使用索引来访问其元素，就像访问列表元素一样。
例如：有一个大小不应改变的矩形，我们设置其长度和宽度，存储在元组中。
>>> print(dimensions[0])
200
>>> print(dimensions[1])
50
>>> dimensions[0]=250
Traceback (most recent call last):
  File "<pyshell#5>", line 1, in <module>
    dimensions[0]=250
TypeError: 'tuple' object does not support item assignment
你看，这里企图修改dimensions元组就被阻止了。

2.遍历元组中的所有值。
